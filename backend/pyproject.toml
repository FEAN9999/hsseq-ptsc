[project]
name = "hseq-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "sqlalchemy>=2.0.36",
  "alembic>=1.14",
  "psycopg[binary]>=3.2",
  "pydantic>=2.12",
  "pydantic-settings>=2.6",
  "pwdlib[bcrypt]>=0.2",
  "pyjwt>=2.10",
]

[project.optional-dependencies]
dev = ["pytest>=8.3", "httpx>=0.28"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
  "error::DeprecationWarning",
  # starlette's TestClient vẫn tham chiếu alias cũ anyio.abc.BlockingPortal,
  # bản thân anyio đã deprecate alias đó — lỗi ở thư viện bên thứ ba, không phải code của mình.
  "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning",
]
